#ifndef VKCPP_UNORDERED_MAP_H
#define VKCPP_UNORDERED_MAP_H

#include <map>

namespace std {

template <typename Key, typename T, typename Hash = void, typename KeyEqual = void>
using unordered_map = map<Key, T>;

}  // namespace std

#endif  // VKCPP_UNORDERED_MAP_H
